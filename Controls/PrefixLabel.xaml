<UserControl x:Class="PlayRandom.Controls.PrefixLabel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PlayRandom.Controls"
             d:DataContext="{d:DesignInstance local:PrefixLabel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel LastChildFill="True">
        <TextBlock DockPanel.Dock="Left" Width="{Binding LabelWidth, Mode=OneWay}" Text="{Binding Text, Mode=OneWay}" VerticalAlignment="Center" HorizontalAlignment="Left" Padding="0,0,5,0"/>
        <StackPanel x:Name="ContentPanel"/>
    </DockPanel>
</UserControl>
